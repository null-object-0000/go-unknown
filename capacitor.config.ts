import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'site.snewbie.go.unknown',
  appName: 'Go Unknown',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
